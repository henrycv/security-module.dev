{% extends 'base.html.twig' %}

{% block body %}
<div class="sm-user-crud">
    <h1>Role creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-primary" />
        <a href="{{ path('role_index') }}" class="btn btn-link">
            Back to the the list
        </a>
    {{ form_end(form) }}
</div>
{% endblock %}
